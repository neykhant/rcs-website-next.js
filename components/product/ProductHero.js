import ProductHeroShared from "../../share-components/ProductHeroShared";

function ProductHero({ featuredProducts }) {
  return <ProductHeroShared featuredProducts={featuredProducts} />;
}

export default ProductHero;
